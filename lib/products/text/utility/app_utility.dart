import 'package:flutter/material.dart';

class AppUtility {
  //Colors
  Color backgroundColor = const Color.fromRGBO(41, 56, 85, 100);
  Color textColor = Colors.white;
  Color cleanedButtonColor = const Color.fromRGBO(168, 188, 255, 200);

  //Padding
  EdgeInsets titlePadding = const EdgeInsets.only(top: 60, left: 16);
  EdgeInsets subscriptionsPadding =
      const EdgeInsets.only(top: 28.0, left: 16, right: 16);
}
